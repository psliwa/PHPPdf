language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - hhvm

before_script:
  - php vendors.php

script: phpunit --configuration tests/phpunit.xml

matrix:
  allow_failures:
    - php: hhvm
